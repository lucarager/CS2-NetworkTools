- Parallel Tool: Added directionality option
- Slope Tool: Introduced custom, unified slope tooltips instead of using two of the base game ones.
- Supernode: Don't allow creating supernodes between networks of different owners. Fixes CTD when attempting to create a supernode that connects a road to a building's path, for example
- All: Fixed invisible path rendering not disabling itself when exiting tools
- All: Fixed asset glitches such as logo being overwritten
- All: Target/View/Snap options will now remember their state when switching tools or reopening tools
- All: Invisible paths are not targets by default now